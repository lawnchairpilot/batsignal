# Xcode
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-to-trash
*.xcuserstate
*.xcscmblueprint
*.xcscheme

# Xcode Derived Data
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/
Packages/

# CocoaPods (if you ever use it)
Pods/
*.xcworkspace
!*.xcodeproj/project.xcworkspace/

# Carthage (if you ever use it)
Carthage/Build/

# Firebase / Google Services
# Do NOT ignore GoogleService-Info.plist if you want teammates to build —
# but if solo and treating it as a secret, uncomment the next line:
# GoogleService-Info.plist

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes

# VS Code
.vscode/

# Environment / secrets
# `.env*` is the one that matters: `*.env` alone matches foo.env but NOT
# `.env`, `.env.local` or `.env.boolsignal`, which is how the last one ended
# up committed.
.env*
*.env
secrets.plist

# Service account private keys. The Firebase console names its downloads
# `<project>-firebase-adminsdk-<hash>.json`, which is a real private key with
# full admin access to the project — it bypasses both firestore.rules and
# storage.rules. Match the shape rather than the one filename, since every
# fresh download from the console arrives with a different hash.
*-firebase-adminsdk-*.json
serviceAccountKey.json

# Firebase CLI debug output — noisy, and it transcribes whatever the CLI was
# doing at the time.
firebase-debug*.log
firebase-debug*.json
.firebase/

node_modules/
